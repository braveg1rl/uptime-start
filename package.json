{
  "name": "uptime-start",
  "version": "0.1.0",
  "author": "Braveg1rl <braveg1rl@outlook.com>",
  "description": "Node's counterpart for performance.timing.navigationStart",
  "keywords": [],
  "license": "MIT",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "coffee-script": "^1.12.2",
    "mocha": "^3.2.x",
    "pre-commit": "^1.2.2"
  },
  "optionalDependencies": {},
  "engines": {
    "node": "6.x.x"
  },
  "main": "lib/uptime-start.js",
  "scripts": {
    "build": "mkdir -p lib && rm -rf lib/* && node_modules/.bin/coffee --compile -m --output lib/ src/",
    "jumpstart": "mkdir -p src && touch src/uptime-start.coffee && mkdir -p test && touch test/uptime-start.coffee && npm install",
    "prepublish": "npm test",
    "pretest": "npm run build",
    "reinstall": "rm -rf node_modules/ && npm install",
    "test": "node_modules/.bin/mocha",
    "watch": "node_modules/.bin/coffee --watch --compile --output lib/ src/"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/braveg1rl/uptime-start.git"
  }
}
